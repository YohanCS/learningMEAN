extends layout 

block content 
    h1 #{title}
    ul
        each article, i in articles
            li= article.title
            p #{article.author} 
            p #{article.body} 

            br
